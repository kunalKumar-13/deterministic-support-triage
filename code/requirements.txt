# Pinned for determinism and minimal install footprint.
# All deps are pure-Python or have prebuilt wheels for Linux/macOS/Windows.

pydantic>=2.6,<3
rank_bm25>=0.2.2,<1.0
scikit-learn>=1.3,<2.0
numpy>=1.24,<3.0
structlog>=23.1,<26.0

# Optional LLM clients - the pipeline runs without these installed.
anthropic>=0.34,<1.0 ; python_version >= "3.9"
openai>=1.30,<2.0   ; python_version >= "3.9"

# Test-only
pytest>=7.4,<9.0
